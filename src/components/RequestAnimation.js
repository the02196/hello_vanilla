import React from 'react'
import styled from 'styled-components'

function RequestAnimation() {
  // let observer = new IntersectionObserver(()=>{})
  // observer.observe()
  
  return (
    <div>RequestAnimation</div>
  )
}

export default RequestAnimation